#!/bin/sh

# SPDX-FileCopyrightText: Copyright © 2024 Caleb Cushing
#
# SPDX-License-Identifier: MIT

asdf install
PATH="./.share/bin:$PATH"
export PATH

if ! git config core.hooksPath 2>/dev/null | grep -q ".share/git/hooks"; then
  echo "⚠️  Git hooks not configured. Run: yarn contribute"
fi
